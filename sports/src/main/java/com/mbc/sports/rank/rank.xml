<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mbc.sports.rank.RankService">
	 <select id="getImage" resultType="string">
	 	select pimage from player_baseball where pname= #{param1} and tname = #{param2}
	 </select>
</mapper>